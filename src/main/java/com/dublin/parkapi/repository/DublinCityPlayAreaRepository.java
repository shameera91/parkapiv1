package com.dublin.parkapi.repository;

import com.dublin.parkapi.model.DublinCityPlayArea;
import org.springframework.data.jpa.repository.JpaRepository;

public interface DublinCityPlayAreaRepository extends JpaRepository<DublinCityPlayArea,Long> {
}
